const student = {
    firstName: "Emily",
    lastName: "Smith",
    age: 23,
};

console.log(`Variable is type ${typeof student}`);
console.log(`Your name is ${student.firstName} ${student.lastName} and you are ${student.age} years old!`);